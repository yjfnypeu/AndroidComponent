apply plugin: 'com.android.library'
apply from: "../../gradle_component/processor.gradle"
apply from: "../../gradle_component/baseconfig.gradle"

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(":baselib")
}
