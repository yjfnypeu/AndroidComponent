apply plugin: 'com.android.application'
apply from: "${GRADLES.baseConfig}"

android {
    defaultConfig {
        applicationId "com.haoge.component.demo"
    }
}

dependencies {
    compile project(":zhihu:component")
    compile project(":core:corecomponent")
}
