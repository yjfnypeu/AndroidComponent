apply plugin: 'com.android.library'
apply from: "${GRADLES.baseConfig}"

dependencies {
    api  'com.android.support:appcompat-v7:25.1.0'

    api  "${DEVDEPS.parceler}"
    api  "${DEVDEPS.router}"
    api  "${DEVDEPS.butterknife}"
    api  'org.greenrobot:eventbus:3.0.0'
}