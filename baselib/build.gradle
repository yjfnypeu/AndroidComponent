apply plugin: 'com.android.library'
apply from: "${GRADLES.baseConfig}"

dependencies {
    compile  "${DEVDEPS.parceler}"
    compile  "${DEVDEPS.router}"
    compile  "${DEVDEPS.butterknife}"
    compile  "${DEVDEPS.datasupport}"
    compile  "${DEVDEPS.fastjson}"
}