// 将所有的apt插件库统一加入。
apply plugin: 'android-apt'
apply plugin: 'com.jakewharton.butterknife'
dependencies {
    apt "${DEVDEPS.parcelerApt}"
    apt "${DEVDEPS.routerApt}"
    apt "${DEVDEPS.butterknifeApt}"
}