// 将所有的apt插件库统一加入。
apply plugin: 'com.jakewharton.butterknife'
dependencies {
    annotationProcessor "${DEVDEPS.parcelerApt}"
    annotationProcessor "${DEVDEPS.routerApt}"
    annotationProcessor "${DEVDEPS.butterknifeApt}"
}